{
  "name": "sbfb-daisyui-app",
  "version": "0.1.0",
  "private": true,
  "description": "Build-time tooling for an SBFB daisyUI + anime.js app. RUNTIME has zero dependencies — everything ships vendored in the archive (app.css + vendor/anime.umd.js).",
  "license": "AGPL-3.0",
  "scripts": {
    "vendor:anime": "node ./scripts/vendor-anime.mjs",
    "build:css": "tailwindcss -i ./src/input.css -o ./app.css --minify",
    "build": "npm run vendor:anime && npm run build:css"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.1",
    "@tailwindcss/node": "4.3.1",
    "@tailwindcss/oxide": "4.3.1",
    "animejs": "4.5.0",
    "daisyui": "5.5.23",
    "tailwindcss": "4.3.1"
  }
}
