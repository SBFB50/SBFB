{
  "suite": "sprint81-transport-e3-hot-subscribe-acceptance",
  "kind": "named-live-check",
  "iroh_lock": "iroh =1.0.1 / iroh-docs =0.101.0 / iroh-gossip =0.101.0 / iroh-blobs =0.103.0",
  "date": "2026-07-05",
  "vocabulary_note": "verdict uses the closed T2 vocabulary as a status+diagnosis pair (same shape as the A3/A4/E/E2 artefacts); this is the LIVE half of the Phase E3 hot-join acceptance — the hermetic halves are the three http.rs channel-boundary tests (Normal pushes exactly one JoinPeers, Duress pushes nothing, invalid hex pushes nothing) and the nexus-core-rs TopicSender::join_peers wrapper test (per-peer degradation, never collect-abort)",
  "scoping_note": "per preflight E3 §2.1 the sufficiency of the hot-join fix is code-SUPPORTED but not runtime-PROVEN until this palier runs: on join_peers the subscribed peer emits NeighborUp and replays its outbox of project announcements (runtime.rs), so the fresh subscriber should see the peer's published apps WITHOUT restart and WITHOUT any action from the peer. This palier is the DECIDER for that hypothesis. It does NOT cover: 'Tes sources'/from_subscribed classification at hot-subscribe (accepted Phase C residual — the NodeDirectory announcement is live-only, never replayed from the outbox), the S75 boot-SEED-driver re-drive carry (orthogonal), and reconnect-after-drop (join_peers dials now but does not extend the boot bootstrap set; a link drop mid-session re-bootstraps only at reboot — keep the observation window in one continuous session)",
  "paliers": {
    "hot_subscribe_convergence": {
      "verdict": "PASS",
      "replayed_at": "2026-07-08 (three days after the E3 commit e05338f; binaries at tip 50f05c1 BOTH sides — the E2-era VPS ephemeral binary predated E3, so a fresh rust:1.94-bookworm-class linux release was built in the sbfb-ci container, sha256 a1b5e0350f5f...9668, and scp'd to the rig)",
      "criterion": "node A boots fresh (empty attention set); node B has at least one published app in its outbox; A does POST /api/daemon/curators/subscribe {node_id of B} AT RUNTIME (no restart) -> A's log shows the hot join + dial to B (network_path relay or direct), B's log shows NeighborUp + outbox replay -> GET /api/daemon/browse on A lists B's app as reachable WITHOUT restarting A and WITHOUT any re-publish or action on B; blob fetch byte-integrity (sha256 identical to B's original) closes the palier",
      "rig": "roles inverted-equivalent to the E2 rig, same throwaway-store discipline (real daemon/store untouched on both machines, Phase F blocking rule held): node B (publisher) = ephemeral node on the dev Windows PC (scratchpad home, node 83f1481355d0..., loopback :53892), node A (fresh subscriber) = ephemeral node on the anchor VPS sbfb-eu (/tmp/sbfb-e3, node a213a8b1fbd7..., loopback :39405); discovery stack presets::N0 pre-EOL (allowed by this palier's own diagnosis note; the E3 fix adds no discovery capability)",
      "observed": "B published 'e3-hot-subscribe-acceptance' (sbfb-explorer zip, archive 62a6abbc89bd...) -> published:true, announcement in B's outbox (outbox=1); A booted FRESH ('no existing running.json — fresh daemon boot'), GET browse BEFORE = {\"entries\":[]}; A POST subscribe {curator_pubkey_hex of B} at 20:27:47Z AT RUNTIME (no restart) -> 200 {subscribed_curators:[83f1...]}; B's log 20:27:45Z (clock skew, during the subscribe request): 'gossip: neighbor up — replaying outbox neighbor=a213a8b1fbd7... neighbors=1 outbox=1' — the hot JoinPeers dial from A reached B across WAN and B replayed its announcement WITHOUT any action; A's browse at FIRST poll (+10s): entries=1, project e3-hot-subscribe-acceptance, source=direct, status=reachable, last_probed_at=20:28:11Z, archive_hash=62a6abbc...; blob byte-integrity: A GET /blob-serve/62a6abbc.../index.html -> HTTP 200, sha256 78c8142e2145... BYTE-IDENTICAL to the PC original (same reference hash as the E2 palier); A-side join success is silent by design (only failure logs 'hot join_peers failed', runtime.rs:1875) — the positive A-side proof is the 200 subscribe + the convergence itself. from_subscribed=false on the entry = the accepted Phase C residual (explicitly out of this palier's scope per scoping_note). Ephemeral nodes killed and /tmp/sbfb-e3 purged after the run; prod daemon + relay1 + pkarr1 stayed active untouched throughout",
      "decider_note": "this palier was the DECIDER for the hot-join sufficiency hypothesis (preflight E3 §2.1): CONFIRMED in-vivo — the subscriber sees the peer's published app in <=10s without restart and without any action from the peer. The Phase E3 hot-join feature leaves PROVISIONAL status; the carry P1 'hot-join PROVISIONAL until live PASS' is CLOSED"
    }
  },
  "hermetic_anchors": {
    "http_channel_boundary": "http::tests::subscribe_curator_pushes_hot_join_for_subscribed_peer (CONTROL→GREEN: exactly one GossipCmd::JoinPeers with the subscribed pubkey, nothing else in the channel) + subscribe_curator_in_duress_pushes_no_hot_join (duress early-return keeps the channel EMPTY — zero new dials under the decoy key, locks the by-construction single-producer placement) + subscribe_curator_invalid_hex_pushes_no_hot_join (Err arm pushes nothing)",
    "core_wrapper": "gossip::tests::join_peers_skips_bad_ids_and_enqueues_valid — empty list short-circuits Ok; a valid unreachable id enqueues Ok (membership hint, not connect-await); a garbage id in a batch is skipped per peer (warn) without aborting the valid one"
  },
  "residual_risk": "if the live palier shows the browse staying empty DESPITE the dial (e.g. B's outbox empty at that instant, receiver-readiness race), reopen the analysis as a timing race — NOT as the (b) pull-directory path, which is mechanically inapplicable at first subscribe (no anchors.json locator exists for B yet, iroh_runtime.rs repull gate); unsubscribe asymmetry documented (iroh-gossip exposes no leave verb; the joined peer stays a HyParView neighbour until churn, ingest already dropped by the is_subscribed gate) — Phase G doc carry"
}
