# Sprint 60 Phase D - LT-7 Tier 3 + Installer Report

Date: 2026-05-12
Status: INSTALLERS VALIDATED / LT-7 P2P INFRA VALIDATED / WORKER QUORUM CARRY

This report separates two Phase D tracks:

- Cross-platform installer validation: Windows NSIS, Linux `.deb`, and macOS `.dmg` are validated with concrete artefact evidence below.
- LT-7 Tier 3 build quorum: not run yet. It still requires simultaneous gossip connectivity across the selected 3-machine set.

## Installer Summary

| Platform | Format | Artefact | Size | Arch | Status |
| --- | --- | --- | --- | --- | --- |
| Windows | NSIS `.exe` | `nexus-launcher_1.0.0_x64-setup.exe` | 16.81 MB | x86_64 | PASS |
| Linux | `.deb` | `nexus-launcher_1.0.0_amd64.deb` | 26.04 MB | amd64 | PASS |
| macOS | `.dmg` | `SBFB Nexus Grid_1.0.0_aarch64.dmg` | 23.32 MB bytes / 22 MB ls | arm64 | PASS |
| Linux | AppImage | n/a | n/a | n/a | SCOPE CUT post-v1.0: linuxdeploy/FUSE blocker |

## Windows NSIS

Evidence source: Sprint 60 Phase C commit `ed5cb69` and local artefact.

Artefact:

- Path: `target/release/nexus-launcher_1.0.0_x64-setup.exe`
- Bytes: `16806226`
- Decimal MB: `16.81`
- SHA256: `959caa1e34db935d6caeb0a1dd803c0cf096bda176a5d148a765cd8bd8d38b7f`

Validation recorded by `ed5cb69` / Phase C review:

- NSIS current-user installer generated by `cargo-packager`.
- Silent install tested with `/S`.
- Install target: `$LOCALAPPDATA\SBFB Nexus Grid`.
- Installed binaries: `nexus-launcher.exe`, `nexus-shell-daemon.exe`.
- Bundled frontend: `web/` directory present.
- Start Menu shortcut present.
- Uninstall registry entry present under HKCU with `DisplayName` and `UninstallString`.
- Silent uninstall removes shortcuts and registry entry.

Known carry:

- P2 S61: NSIS multi-binary uninstall can leave residual files if binaries are locked at uninstall time.
- P3: Authenticode signing / SmartScreen trust remains post-v1.0.

## Linux `.deb`

Evidence source: commit `b6a93a8` and local artefact.

Artefact:

- Path: `target/release/nexus-launcher_1.0.0_amd64.deb`
- Bytes: `26043988`
- Decimal MB: `26.04`
- SHA256: `022da75afdfe1a11897bb64ffd830bdc7b1b0316288a46ce69eaabeca01d49b4`

Validation recorded by `b6a93a8`:

- Environment: Docker `sbfb-ci`, Debian 13, Rust 1.95.
- `.deb` produced: `nexus-launcher_1.0.0_amd64.deb`.
- `dpkg -i` installs successfully.
- Installed binaries:
  - `/usr/bin/nexus-launcher` around 5.7 MB.
  - `/usr/bin/nexus-shell-daemon` around 26 MB.
- Bundled frontend present at `/usr/lib/nexus-launcher/web/index.html`.
- `nexus-launcher --help` prints help correctly.
- `dpkg -r sbfb-nexus-grid` uninstalls cleanly.

AppImage:

- `linuxdeploy` failed in Docker because FUSE is unavailable.
- AppImage is explicitly scope-cut post-v1.0.

## macOS `.dmg`

Evidence source: SSH to `mac` on 2026-05-12.

System:

- `ProductName`: macOS
- `ProductVersion`: `26.3.1`
- `BuildVersion`: `25D2128`
- `uname -m`: `arm64`
- `uname -sr`: `Darwin 25.3.0`

Artefact:

- Path: `/Users/theophilevasseur/nexus/target/release/SBFB Nexus Grid_1.0.0_aarch64.dmg`
- `ls -lh`: `22M`
- Bytes: `23324465`
- SHA256: `918863e1d0a81129a241ef205dd71e0dbb2097f68d4f78ded511092b39f81b48`
- `hdiutil imageinfo`: UDIF read-only compressed zlib, `Format: UDZO`, GUID partition scheme.

Installed app:

- Path: `/Applications/SBFB Nexus Grid.app`
- Owner/group: `theophilevasseur admin`
- Bundle size: `71M`
- Bundle files observed:
  - `Contents/Info.plist`
  - `Contents/MacOS/nexus-launcher`
  - `Contents/MacOS/nexus-shell-daemon`
  - `Contents/Resources/SBFB Nexus Grid.icns`
  - `Contents/Resources/web/index.html`
  - `Contents/Resources/web/sbfb-bridge.js`

Embedded binaries:

- `Contents/MacOS/nexus-launcher`
  - Size: `4.3M`
  - File type: `Mach-O 64-bit executable arm64`
  - SHA256: `f1079302e39d4a1466a754f6b5e074821ad4f3ab0c6f93368ba5a141d20e6842`
- `Contents/MacOS/nexus-shell-daemon`
  - Size: `18M`
  - File type: `Mach-O 64-bit executable arm64`
  - SHA256: `c3899e77e7f7ae6328caafb380bb6699c0c380563a454c17b05ee3410ec52e18`

Bundled frontend:

- Path: `/Applications/SBFB Nexus Grid.app/Contents/Resources/web/index.html`
- Size: `1.3K`
- SHA256: `1c01a1636bec5936509b471428080c8f33e16a6195da8e77cbce84960668ffcd`

Run validation:

- `nexus-launcher --help` executed from the app bundle and printed the expected launcher help.
- SSH process check observed a running launcher from `/Applications/SBFB Nexus Grid.app/Contents/MacOS/nexus-launcher`.
- Manual GUI validation by the user: the app was placed in Applications, launched successfully, and opened the browser.

Uninstall model:

- macOS packaging uses a standard `.app` bundle installed by drag/copy to `/Applications`.
- No service registration, launch daemon, or registry-equivalent install step is recorded by this package.
- Destructive drag-to-trash removal was not executed over SSH to preserve the currently installed test app.

## LT-7 Tier 3 Quorum

Status: **GOSSIP + API + TASK SUBMIT VALIDATED / WORKER QUORUM CARRY POST-TAG**.

### Gossip connectivity (VALIDATED 2026-05-12)

Three daemons started simultaneously, gossip connections observed:

| Machine | Location | OS | Arch | node_id (prefix) |
|---|---|---|---|---|
| Win dev | Paris (LAN) | Windows 11 | x86_64 | `fe7a4898a1` |
| VPS Helsinki | Hetzner | Debian 13 | x86_64 | `6592b2be01` |
| Mac | Paris (LAN) | macOS 26.3.1 | arm64 | `6944503a3c` |

VPS daemon log — gossip connections to both peers:

```
2026-05-12T17:22:15 router.accept{me=6592b2be01 alpn="/iroh-gossip/1" remote=fe7a4898a1}
  → Win ↔ VPS gossip active (IP 104.28.158.14 + IPv6 2a09:bac1:...)
2026-05-12T17:22:19 gossip{me=6592b2be01 alpn=/iroh-gossip/1 remote=6944503a3c}
  → Mac ↔ VPS gossip active (IP 176.150.28.203 + IPv6 2001:861:...)
```

### API mutual discovery (VALIDATED 2026-05-12)

Auth corrigee : le header est `x-sbfb-token` (pas `Authorization:
Bearer`), et `GET /auth/token` retourne le token courant sans auth.
Erreur initiale de diagnostic (utilisait le mauvais header).

Chaque daemon voit les 2 autres dans `subscribed_curators` :

```
Win:  subscribed_curators = [6592b2be01 (VPS), 6944503a3c (Mac)]
VPS:  subscribed_curators = [fe7a4898a1 (Win), 6944503a3c (Mac), 1a96e287c5]
Mac:  subscribed_curators = [fe7a4898a1 (Win), 6592b2be01 (VPS), 1a96e287c5]
```

### Task submission (VALIDATED 2026-05-12)

Task soumise via `POST /api/v1/tasks/submit` sur le daemon Win :

```json
{
  "project_id": "lt7-tier3-test",
  "task_type": "inference",
  "prompt": "Hello LT-7 Tier 3 quorum test",
  "model": "echo",
  "priority": 5
}
```

Reponse : task ID `000000006a03641e-e46a17bd431cfec2`, signee
Ed25519, `redundancy_factor: 1`, persistee dans coordinator.db.

### Worker quorum E2E (CARRY post-tag)

Le chemin valide : gossip 3 machines ✓ → API auth ✓ → task submit ✓.

Le maillon manquant : **workers** (nexus-worker binary) qui claim
la tache, executent le build, et soumettent le SHA256 via
`/api/v1/results/submit`. Le quorum validator compare les SHA256
et fait la transition AwaitingQuorum → Completed.

Raison du carry : les workers ne sont pas deployes sur VPS/Mac
(seul le daemon y tourne). Le worker necessite Ollama (pour les
inference tasks) ou le build_executor (pour les build tasks).
Deployer et configurer les workers sur 3 machines heterogenes
depasse le timebox de la session.

Reclassification :
- **Tier 3 P2P infrastructure** : VALIDATED (gossip + API + task submit cross-machine)
- **Tier 3 worker quorum** : carry post-tag. Tier 1+2 quorum
  logic validated by unit tests (S55 Phase C, 1259 nextest green).

Important limitation (D4) :
- Reproducible Rust build outputs are meaningful inside the same
  OS/toolchain triplet. Cross-OS byte-identical build consensus
  remains a stretch goal, not the MVP proof.
