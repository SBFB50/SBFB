# Sprint 76 Phase B Preflight

Date: 2026-06-15
HEAD: `d6dea45`
Verdict: **SCOPE-CUT-CONSISTENT**

> Phase B = dette reservee NON convertible (sprint PAIR, Regle 1 G7). Le P1
> `DURESS-BOOT-LEAK` etant deja ferme (`23a08c9`), B est duress local-only +
> 3 carries 2-reports anti-escalade + tests/refacto/doc. Aucun finding bloquant
> S1b/S2/S3/S4. Plusieurs derives de line-numbers (Phase A `ce43894` a modifie
> `http.rs`/`runtime.rs`), une derive de scope mineure (B9 « 5 pages » → 4 reelles),
> et un point de profondeur sur B1 (publisher-binding observed = pubkey
> non-authentifie a cette couche). Tous non-bloquants, documentes ci-dessous.

## Evidence Rules
- Claim policy : chaque claim cite un path:line, une sortie de commande, une URL/date, ou une hypothese explicite.
- Local sources read : `prompts/agent/preflight.md` ; `.planning/active/sprint76_plan.md` (§5 B.1-B.5) ; `.planning/active/sprint76_kickoff.md` (§6/§7) ; `.planning/active/sprint75_audit_findings.md` ; `crates/nexus-shell-daemon/src/noop_identity.rs` ; `crates/nexus-shell-daemon/src/http.rs` (1529-1578, 1727-1856, 2066-2242, 240-256, 2660-2718) ; `crates/nexus-shell-daemon/src/runtime.rs` (1640-1810, 2015-2032, 2144) ; `crates/nexus-shell-daemon/src/feed_sync.rs` (160-200) ; `crates/nexus-shell-daemon-core/src/browse.rs` (570-588) ; `crates/nexus-shell-daemon-core/src/iroh_runtime.rs` (1092-1176, 863-933) ; `crates/sbfb-manifest/src/lib.rs` (52-63) ; `web/src/bridge/protocol.ts` (20-44) ; `web/src/pages/Nodes.tsx` (155-175, 340-355) ; `web/vitest.config.ts` (73-77) ; `web/package.json` ; `.github/workflows/ci.yml` (78-90) ; `docs/security/LOOPBACK_ENDPOINTS_TRUST_TIERS.md` (62-104) ; `docs/security/THREAT_MODEL.md` (19, 876-884, 882) ; `crates/nexus-core-rs/src/canonical.rs` (50-239) ; memory `feedback_approach.md`, `nexus_grid_pivot.md`.
- Commands run : `git show 23a08c9` ; `git log --oneline 23a08c9..HEAD -- http.rs` (= `ce43894` seul) ; `grep -c FORMAT_VERSION crates/nexus-core-rs/src/*.rs` ; `grep DOMAIN_/ANNOUNCEMENT_VERSION` ; WebSearch (duress decoy / BitTorrent failover / ingress trust-boundary / iroh-blobs Downloader, juin 2026).

## Scope
- Plan source : `.planning/active/sprint76_plan.md` §5 (lignes 215-303), 11 items B1-B11, 15 tests B.3.
- Target files (ancres reelles confirmees/corrigees) :
  - `crates/nexus-shell-daemon/src/http.rs` — `set_keep_online` **1529** (plan disait 1529-1577 OK), `seed_voluntary` **2066** (plan disait :2066/2206, OK), failover B3 **2099-2189** (OK), blob-serve public route **248-255**, `trustworthy_open_source` helper **2693-2706**.
  - `crates/nexus-shell-daemon/src/runtime.rs` — ingest aggregator B2 **2252** (`is_open_source: ann.is_open_source`, **PAS 2231** : 2231 est un `debug!` de parse-ticket, ligne derivee par Phase A) ; `GossipCmd::Outbox` handler B4 **1790** (plan disait 1787, +3) ; replay/`remint_and_wrap_for_replay` + `my_endpoint_addr()` B5 **1654-1810 / 2015-2032 / 2144** (plan disait 1655-1850, OK approx).
  - `crates/nexus-shell-daemon-core/src/iroh_runtime.rs` — observed capture B1 **1166** (`record_observed_directory`, def **863**) ; **PAS `iroh_runtime.rs:507-521`** (le plan/audit citaient un chemin/ligne faux : c'est `nexus-shell-daemon-core`, et la capture est a 1166).
  - `crates/nexus-shell-daemon-core/src/browse.rs` — `add_direct_entry` **575**, `get_direct_entry` **584** (cible reelle du fix B2 ingress).
  - `web/src/pages/Nodes.tsx` — `waiting`/`WaitingRow` B6 **160-348** (le plan disait 164,347, OK).
  - `crates/sbfb-manifest/src/lib.rs` — `BRIDGE_METHOD_ALLOWLIST` B10 **52-63** (10 methodes).
  - `web/src/bridge/protocol.ts` — `BridgeMethodSchema` B10 **20-44** (15 methodes).
  - Docs : `docs/security/LOOPBACK_ENDPOINTS_TRUST_TIERS.md` §3, `docs/security/THREAT_MODEL.md` §15.1, `.github/workflows/ci.yml` [10].
- Deps/APIs/specs : **AUCUNE nouvelle dep attendue**. Risque dep unique = B9 Playwright runner — **deja present** (`@playwright/test ^1.59.1` dans `web/package.json:51`, script `test:e2e`). Voir S1b.
- Security/protocol surfaces : duress identity model (B1), trust boundary ingress aggregator (B2), blob-serve public route + amplification (B8), bridge allowlist (B10). **0 struct wire/canonical touchee.**
- Tests expected : 15 (B.3 #1-#15). Acceptance §B.4 : `cargo nextest run -p nexus-shell-daemon -p nexus-shell-daemon-core --locked` + web `test:unit`+`test:coverage` (>= 85/85/78/85) + LOOPBACK §3 mis a jour + `grep -c FORMAT_VERSION` inchange.

## S1a OSS Prior Art
- Domain : (B1) duress/decoy no-op + reponse benigne plausible ; (B2) validation a l'ingress sur une frontiere de confiance ; (B3) failover multi-source cross-tier sur P2P content-addressed.
- Sources (juin 2026) :
  - VeraCrypt Hidden OS (decoy/hidden coexistence, indistinguable sous duress) — deepwiki.com/veracrypt/VeraCrypt/4.3-hidden-operating-systems ; Signal « simplifying OTR deniability » signal.org/blog/simplifying-otr-deniability/ ; USPTO 8782409 « benign, context-aware cover message » (decoy = reponse benigne plausible). **Confirme : un decoy doit produire une reponse benigne indistinguable, tout en supprimant les effets de bord reels.**
  - CERT IDS00-J « Sanitize untrusted data passed across a trust boundary » (informit.com) + arc42 Input-Sanitization (quality.arc42.org) : valider/transformer **au chokepoint de frontiere** (message payloads), pas par-consommateur. **Confirme le pattern B2** (single chokepoint partage index+/browse).
  - iroh-blobs `Downloader`/`ContentDiscovery::find_providers` (docs.rs/iroh-blobs, blog 0.90/0.95) : le downloader essaie chaque node d'une **sequence de providers ordonnee** et abandonne en fin de stream fini. **Confirme : le failover INTRA-vecteur existe deja ; PULL-3 vise le CROSS-tier** (basculer d'un plan `Ticket` mort vers le tier directory/multi-provider, non couvert par le downloader puisqu'un ticket unique n'est pas une sequence).
- Finding : **APPROACH-ALIGNED** pour B1 (miroir `run_boot_seed_driver` = early-return + 200/leurre, exactement le pattern decoy benin), B2 (ingress trust-boundary sanitize = pratique CERT/arc42), B3 (le gap cross-tier est reel et le remede — chainer ticket→directory→multi sur echec — est conforme a la composition de sources OSS). Aucun `APPROACH-NAIVE`, aucun `LIB-EXISTS` (les primitives iroh-blobs sont deja utilisees ; B3 est de l'orchestration applicative au-dessus).
- Impact : aucune adaptation requise.

## S1b Dependencies, CVEs, Release Notes
- Scanned : aucune dep Rust ajoutee/bumpee par B1-B11 (items Rust = logique locale + tests + refacto ; aucun `[dependencies]` touche). Front B9 = Playwright.
- Commands/sources : `web/package.json` → `@playwright/test ^1.59.1` (devDependency) **deja installe**, `test:e2e` script present, `ci.yml` [10] l'invoque deja (`xvfb-run npx playwright test`). `@vitest/coverage-v8 ^4.1.4` present (B9 coverage). `grep -c FORMAT_VERSION crates/nexus-core-rs/src/*.rs` = baseline a re-grepper identique en acceptance.
- Transitive : N/A — **aucun bump** ⇒ pas de risque de collision majeure (lecon S72 schemars 0.8/1.2 non applicable : rien n'est ajoute). Si B9 retient l'option « ≥1 spec Playwright reel », un `playwright.config.ts` net-new est requis (config, pas dep). Si l'option « retirer l'etape CI [10] » est retenue, considerer retirer aussi `@playwright/test` devDep (hygiene, optionnel).
- Finding : **clean**. (Aucune CVE crypto/wire/network introduite ; 0 dep nouvelle.)

## S2 Historical Decisions
- Commands : `git show 23a08c9 --no-patch --format=%B` ; `git log --oneline 23a08c9..HEAD -- crates/nexus-shell-daemon/src/http.rs` (= `ce43894` Phase A SEUL) ; `grep DURESS-FRERES/CARRY-3/PULL-3/LOOPBACK-TIERS .planning`.
- Decisions crossed :
  - **B1 DURESS-FRERES-LOCAL** : `23a08c9` (fix P1 wire-emit) declare EXPLICITEMENT dans son body : « le residuel des freres LOCAL-ONLY (`seed_voluntary`, `set_keep_online`) reste P2 lot S76 et descend de M a L ». THREAT_MODEL row 884 le documente identiquement. **Reverse-commit check** : aucun commit `23a08c9..HEAD` ne reverse/contredit cette planification (seul `ce43894` touche http.rs, sans annuler le scope duress). → **confirmed continuation, non-blocking**. Le pattern impose : early-return court-circuit + reponse leurre coherente, miroir `run_boot_seed_driver` (http.rs:1739-1743 : `if gossip_publish_in_duress(state.identity_mode) == Noop { return 0; }`).
  - **B2 CARRY-3** : audit S75 §5 (lignes 326-338) adjudique le residu canal `direct` comme **REEL mais P2** (pas P1), re-route S76, remede connu = downgrade `trustworthy_open_source` a l'ingress aggregator. Le skeptic #1 a REFUTE la variante NodeCatalog (distincte). → la decision historique **demande** le fix B2, ne le rejette pas.
  - **B3 PULL-3** : audit S75 Track E (ligne 163-164) « genuinement absent et honnetement trace P2 → S76 ». Aucune decision « ne pas faire ». → demande.
  - **B7 LOOPBACK-TIERS** : audit ligne 357/430-433 « drift cumule 2 sprints — proche du seuil 3+ (escalade G7 si non traite) ». → demande, anti-escalade.
  - **B8 THREAT-BLOBSERVE-BEARER** : audit ligne 356/427-429 corrige une cellule THREAT_MODEL fausse. → correction doc, non-controversee.
  - **Scope cuts gelees** (kickoff §7) : SYBIL-SEEDER-TAIL **reconduit S77** avec exemption « dependance interne sharding » nommee (kickoff lignes 798/812) — B ne le traite PAS, conforme. Pas de Day 0 re-debattue.
- Finding : **clean** (toutes les decisions historiques traversees DEMANDENT les items B ; 0 reversion non-confirmee, 0 rationale valide contredite).

## S3 Local Patterns And Threat Model
- Threats/contracts checked :
  - **B1 duress (T modele duress)** : THREAT_MODEL row **882** (« Boot feed-emit sous duress », P1 ferme `23a08c9`, residual Nil) + row **884** (freres LOCAL-ONLY, residual **L**, « lot dette duress route audit S76 »). B1 ne regresse RIEN : il **ferme** le gap documente row 884. Consequence doc attendue : abaisser le residual row 884 L→Nil apres B1. Le pattern de gate est centralise dans `noop_identity.rs` (le doc-comment lignes 38-41 invite explicitement a router toute route duress-sensible par ce module — green-flag reviewer). B1 doit gater AVANT toute mutation : `set_keep_online` (DB `db.set_keep_online` http.rs:1546 + tag blob) et `seed_voluntary` (fetch+pin+`db.set_keep_online` http.rs:2197 + **emit SeedAnnounced** http.rs:2206). Note : le wire-emit de `seed_voluntary` est deja protege par la philosophie `gossip_publish_in_duress` ; un early-return en tete couvre A LA FOIS le local-mutate ET l'emit.
  - **B1 publisher-binding observed** : la capture `record_observed_directory(ann_pubkey, ...)` (iroh_runtime.rs:1166) enregistre `ann_pubkey` = `node_pubkey_hex` reclame, **deja documente non-authentifie** (commentaire 1158-1160 : « the claimed pubkey is unauthenticated »). Le self-guard echo/forge existe (1161-1164). **Point de profondeur (non-bloquant)** : le PoW de l'enveloppe gossip est lie a (publisher, topic), PAS au `node_pubkey_hex` interne (cf. memory UX-ARRIVAL « le PoW gossip est lie a (publisher, topic) PAS au payload »). « Lier l'observed a l'identite PoW publisher » signifie soit (a) deriver `ann_pubkey` du `delivered_from`/auteur d'enveloppe verifie au lieu du champ payload auto-declare, soit (b) documenter honnetement que l'observed reste availability-only/non-securitaire (registre borne+rate-limite §P59.2 = la vraie defense anti-flood). L'option (b) est coherente avec le residual deja assume ; (a) exige que l'auteur d'enveloppe soit disponible a la couche `process_directory_announcement_bytes` (il ne l'est pas aujourd'hui — verifie au call-site runtime.rs:1960 qui ne passe que `content`/`node`). **A trancher en implementation**, sans blocage : c'est un durcissement availability-only, pas une regression.
  - **B2 trust boundary** : `index_browse_entry` (http.rs:2679-2706) applique deja `trustworthy_open_source` POUR L'INDEX. Mais `ingest_project_announcement` (runtime.rs:2252) construit l'entry avec `is_open_source: ann.is_open_source` VERBATIM puis `browse_aggregator.add_direct_entry(entry)` (runtime.rs:2260) stocke la valeur NON-downgradee → `/browse` sert le badge spoofable. Le fix B2 doit appliquer le meme calcul a l'entry AVANT `add_direct_entry` (ingress aggregator, le vrai chokepoint partage). Verrou-4 front consomme `/browse` `source==="direct"` → la doc THREAT_MODEL §15.1 doit dire « verrou-4 ≠ attestation crypto ». Non-regression : aucune autre voie ne reduit la garantie ; c'est un renforcement.
  - **B8 blob-serve** : `public_routes` (http.rs:248-255) monte `/blob-serve` SANS bearer/Host/Origin. THREAT_MODEL ligne 876 (§15.1) revendique a tort « loopback bearer requis sur la route ». B8 corrige la cellule (route PUBLIQUE par construction ; amplification bornee par resolution subscribed-only + cap `MAX_FETCH_PROVIDERS`/`PULL_PROVIDER_CAP=8` + timeout). Correction de documentation honnete, pas un changement de comportement.
- HARDENING_ROADMAP status : `grep S76` = aucun pre-requirement HARDENING attache a S76. Phase B ne manque aucun pre-requis de sprint.
- Finding : **non-blocking**. B1/B2/B8 ferment/clarifient des gaps documentes (pas de regression T0-T5). Le seul point a trancher (B1 publisher-binding (a) vs (b)) est availability-only.

## S4 Protocol And Wire Invariants
- Wire/security files checked : `crates/nexus-core-rs/src/canonical.rs` (50-239, tous les `DOMAIN_*` + doc) ; `grep -c FORMAT_VERSION crates/nexus-core-rs/src/*.rs` ; `grep *_FORMAT_VERSION = ...`.
- VERSION/domain/canonical status : tous `*_FORMAT_VERSION = 1` (curator 61, key_rotation 32, node_directory 84, pow 85, seed 51, task 61, tls_pinning 102 ; task_response 48 = 1). Tous `DOMAIN_*` stables (TASK/RESULT/CLAIM/INVITE/KUDOS/CURATOR_LIST/PROVENANCE/CANARY/POW/DURESS_ACK/AGE_WITNESS/CONTRIBUTOR/KEY_ROTATION/DELEGATION/FEED/SEED_REQUEST/SEED_RESPONSE/NODE_DIRECTORY). **AUCUN item B1-B11 ne touche `canonical.rs`, `seed.rs`, `task.rs`, `node_directory.rs`, `curator.rs`** (structs wire) ni un `*_VERSION`.
- Trace producteur → consommateur (P2-PREFLIGHT-WIRE-CONTRACT-DEPTH) :
  - **B1** : seul champ « wire » concerne = `SeedAnnounced` raw-op emis par `emit_seed_announced` (http.rs:2206, producteur) → consomme par l'ingest SeedRegistry. B1 fait que ce producteur **NE FIRE PAS en duress** (early-return avant 2206). **Aucune** mutation de la FORME du payload (cle/null/enveloppe inchangees). C'est une suppression conditionnelle d'emission, identique au fix P1 `23a08c9`. → « unchanged wire » prouve aux deux bouts.
  - **B2** : le champ `is_open_source` est un bool deja present dans `ProjectAnnouncement` (producteur gossip distant, non signe) et `BrowseEntry` (consomme par `/browse` JSON + front verrou-4). B2 ne change PAS la forme — il change la VALEUR calculee a l'ingress (downgrade `true`→`false` si provenance/repo manquants), exactement comme `index_browse_entry` le fait deja pour l'index. Le consommateur front lit toujours un bool `is_open_source`. → unchanged wire shape.
  - **B6/B10** : front/manifest declaratif, pas de champ wire reseau.
- Day 0 status : **preserved**. iroh 0.98 pin (Day 0 #3) intact (0 dep). « Feed raw-op extensible, pas de bump par op » respecte. « `*_ANNOUNCEMENT_VERSION` restent a 1 jusqu'au go-live » respecte. Pre-launch protocol : Phase B = 0 bump wire, tout local/doc/test/refacto.
- Finding : **clean** (aucun `*_VERSION` bump, aucun decoder multi-version tolerant introduit, aucun `serde(default)` nouveau, Day 0 preserve).

## Per-item coherence (B1-B11)

| Item | Ancre confirmee/derivee | Risque | Verdict item |
|---|---|---|---|
| **B1** duress freres + observed binding | `set_keep_online` http.rs:**1529** OK ; `seed_voluntary` http.rs:**2066** OK ; miroir `run_boot_seed_driver` http.rs:**1739-1743** confirme (early-return + Noop) ; observed capture **iroh_runtime.rs:1166** (PAS `iroh_runtime.rs:507-521` — chemin/ligne faux dans plan/audit, c'est `nexus-shell-daemon-core`). Publisher-binding = pubkey **non-authentifie** a cette couche (PoW lie a publisher+topic, pas au payload). | Trancher (a) auteur-d'enveloppe verifie vs (b) doc availability-only. Availability-only, non-securitaire. | EXECUTE (gate avant mutation, miroir exact ; trancher (a)/(b) en impl, defaut (b) honnete). |
| **B2** CARRY-3 ingress sanitize | Ingress reel = **runtime.rs:2252** (`is_open_source: ann.is_open_source`) + `add_direct_entry` **browse.rs:575** ; **PAS runtime.rs:2231** (debug log parse-ticket, derive Phase A). Helper `trustworthy_open_source` **http.rs:2693-2706** reutilisable. | Appliquer downgrade AVANT `add_direct_entry` (runtime.rs:2260), pas seulement a l'index. | EXECUTE (ancre corrigee 2231→2252). |
| **B3** PULL-3 cross-tier failover | Failover INTRA-vecteur (anchor→seeders) **existe** (`SeedFetchPlan::Multi` + `fetch_and_pin_multi` http.rs:2176-2189). Gap reel = CROSS-tier : `SeedFetchPlan::Ticket` mort (echec fetch http.rs:2233) → **pas** de bascule directory. Plan « ticket→directory→multi » exact. | Re-ordonner la selection de plan en chaine d'echec (essayer directory/multi SI ticket echoue), pas seulement a la selection. | EXECUTE (gap precise ; OSS iroh-blobs confirme intra-tier deja la). |
| **B4** T6-OUTBOX-DIRECT | Handler `GossipCmd::Outbox` **runtime.rs:1790** (plan 1787, +3). 0 test direct (`grep -c GossipCmd::Outbox` = 1 = le handler seul). `neighbor_count` track 1739/1807. | Test 2-noeuds pur (pattern S75-A hijack-guard). | EXECUTE. |
| **B5** WS-3/PD-5 hoisting | `my_endpoint_addr()` appele 8×, dont dans les boucles replay (runtime.rs:2032/2577/...) ; `remint_and_wrap_for_replay` **2144**. Region replay **1654-1810**. | Refacto efficience pure ; nextest doit rester inchange (assert par test #7). | EXECUTE. |
| **B6** discriminateur curator/ancre | `Nodes.tsx` `waiting`/`WaitingRow` **160-348** (plan 164,347 OK). 0 wire. | Distinguer curator-pur vs ancre-non-annoncee dans les lignes « en attente ». | EXECUTE. |
| **B7** LOOPBACK-TIERS-STALE | §3 LOOPBACK lignes **62-104** : routes S74/S75 (`/api/daemon/keep-online`, `/api/daemon/seed`, `/api/daemon/seed-count`, `/directory/publish`, `/seed/request`, `/nodes`, `/api/daemon/search`) **absentes**. Doc seul. | Inscrire 7 routes (toutes T0 ; candidats T1 `/directory/publish`+`/seed/request`) + corriger phrase fausse. | EXECUTE (anti-escalade). |
| **B8** THREAT-BLOBSERVE-BEARER | blob-serve **public_routes http.rs:248-255** (sans bearer) vs THREAT_MODEL §15.1 ligne **876** « loopback bearer requis » (FAUX). Doc seul. | Corriger cellule : route publique, amplification bornee (subscribed-only+cap+timeout). | EXECUTE. |
| **B9** lot test front + CI playwright | **DERIVE SCOPE** : plan liste 5 pages 0-test [Network/Curators/Projects/OnboardingEmpty/ProjectDetail] mais **Network.test.tsx EXISTE** (ajoute `e980d7e` UX-ARRIVAL). Pages reellement 0-test = **4** : Curators, OnboardingEmpty, ProjectDetail, Projects. Test #10 `network_page_smoke_renders` redondant. CI [10] = `npx playwright test` SANS `playwright.config.*` ni `*.spec.ts` (0 e2e dir) → green vacuous confirme. Seuils coverage **85/85/78/85** (vitest.config.ts:73-77). Playwright `^1.59.1` installe. | Re-cibler les 4 pages reelles (laisser Network couvert). Choisir option Playwright (≥1 spec reel OU retirer [10]+devDep). | EXECUTE (scope re-cible 5→4 pages, non-bloquant). |
| **B10** BRIDGE-ALLOWLIST-DRIFT | TS `protocol.ts:20-44` = **15** methodes ; Rust `sbfb-manifest/lib.rs:52-63` = **10**. 5 TS-only : `pii_redact`, `storage_version`, `provenance_get`, `provenance_verify`, `feed_cursor_get`. L'allowlist Rust gate `manifest.methods` declaratif (validation manifest), PAS le dispatch runtime (host shell). | Aligner OU test parite + doc « declaratif, pas evasion sandbox ». Les 5 TS-only sont des methodes host-dispatched legitimes → la « parite » doit etre documentee comme intentionnelle, pas forcement identique. | EXECUTE (preferer test parite documentee a un alignement mecanique aveugle). |
| **B11** UX-ARRIVAL inscription | Doc `sprint76_audit_plan.md`. | Inscrire la surface UX-ARRIVAL comme track couvert. | EXECUTE. |

## Plan Adaptation
Non requis (verdict SCOPE-CUT-CONSISTENT, pas PLAN-ADAPT). Aucun `APPROACH-NAIVE`/`LIB-EXISTS`.

## Risks And Scope Cuts
- Blocking risks : **aucun** (0 finding bloquant S1b/S2/S3/S4).
- Non-blocking risks / corrections a porter en implementation :
  1. **Ancres derivees** (Phase A `ce43894`) a utiliser, PAS les line-numbers du plan : B2 ingress **runtime.rs:2252** (pas 2231) ; B1 observed **iroh_runtime.rs:1166** dans `nexus-shell-daemon-core` (pas `iroh_runtime.rs:507-521`) ; B4 outbox **runtime.rs:1790** (pas 1787).
  2. **B9 scope 5→4 pages** : Network deja teste ; cibler Curators/OnboardingEmpty/ProjectDetail/Projects. Test #10 plan redondant → remplacer par un 4e smoke utile ou l'absorber.
  3. **B1 publisher-binding** : pubkey observed non-authentifie a la couche `process_directory_announcement_bytes` (PoW lie publisher+topic). Trancher (a) deriver de l'auteur d'enveloppe verifie (exige plumbing au call-site runtime.rs:1960) vs (b) documenter availability-only (defaut honnete, coherent residual §P59.2 + memory UX-ARRIVAL). Ne pas sur-promettre « borne la forge » au-dela de ce que la couche permet.
  4. **B10** : ne pas aligner mecaniquement Rust(10)→TS(15) ; les 5 TS-only sont host-dispatched legitimes — preferer un **test de parite documentee** affirmant l'invariant (manifest allowlist = declaratif, pas le canal de dispatch).
  5. **B8/B2 consequences doc** : apres B1, abaisser THREAT_MODEL row 884 residual L→Nil ; B2 ajoute la phrase « verrou-4 ≠ attestation crypto » §15.1.
  6. **B3** : le remede est une chaine d'echec (retry cross-tier sur fetch fail), pas seulement une selection de plan — sinon un ticket vivant-mais-dead reste un BAD_GATEWAY terminal.
- Scope cuts honores (kickoff §7) : SYBIL-SEEDER-TAIL **reconduit S77** (exemption « dependance sharding » nommee, kickoff 798/812) — B ne le traite pas, conforme. Sharding (#1) / tensor-split (#2) / VRAM-live (#3) hors B. 4 carries externes (P2-A-1 rand, P2-AUDIT-2 iroh, T-NN+2 wasm, P3-OS-1) non touches.

## Action
- **SCOPE-CUT-CONSISTENT** : proceder Phase B comme planifie, avec les corrections d'ancres (1,2,3 ci-dessus) appliquees a l'implementation et tracees au commit body. Tracker les carry-over non-bloquants. Le commit body Phase B doit citer ce preflight (`## G8 traceability`) et noter les derives d'ancres + B9 5→4 pages + B1 (a)/(b) tranche.
